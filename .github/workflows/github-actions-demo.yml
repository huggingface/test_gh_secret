name: Test Child
on: 
  workflow_call:
    secrets:
      TEST_SECRET:
        required: true
jobs:
  Job1:
    runs-on: ubuntu-latest
    environment: TestEnvironment
    steps:
      - name: Step 1
        run: |
          if [ "${{ secrets.TEST_SECRET }}" == "123" ]; then
            echo "Match"
          else
            echo "Not matched"
          fi
