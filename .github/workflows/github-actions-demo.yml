name: Test Child
on: 
  workflow_call:
    inputs:
      environment:
        type: string
        description: environment to deploy to
        required: true
    secrets:
      TEST_SECRET:
        required: true
jobs:
  Job1:
    runs-on: ubuntu-latest
    environment: prod
    steps:
      - name: Step 1
        run: |
          if [ "${{ secrets.TEST_SECRET }}" == "123" ]; then
            echo "Match"
          else
            echo "Not matched"
          fi
